{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
  "security:audit": "npm run security:audit --prefix backend && npm run security:audit --prefix frontend",
  "security:sast": "npm run security:sast --prefix backend && npm run security:sast --prefix frontend",
  "security:dast": "npm run security:dast --prefix backend",
  "security:check": "npm run security:check --prefix backend && npm run security:check --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
